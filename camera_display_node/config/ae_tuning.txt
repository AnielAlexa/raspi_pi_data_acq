# Active exposure standalone tuning (key=value)
# Used by: ./test_autoexposure_cpp <calib_dir> <method> <tuning_file>
#
# NOTE:
# compute() now mirrors uzh-rpg AutoCameraSettings::calculateNewSettings():
#   1. Call AE method directly (no extra damping/step-limit/blending on top)
#   2. Clamp to [min=20us, max_exposure_ms]
#   3. adjustGain() heuristic

# Hard upper limit for exposure time (ms).
max_exposure_ms = 2.0

# Gain bounds and step.
min_gain_x = 1.0
max_gain_x = 1.5
gain_change_step_x = 0.5

# Hold gain for a few frames after each gain update to prevent ping-pong.
gain_cooldown_frames = 7
