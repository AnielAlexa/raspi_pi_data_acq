# Active exposure standalone tuning (key=value)
# Used by: ./test_autoexposure_cpp <calib_dir> <method> <tuning_file>
#
# NOTE:
# - This file now keeps only ACTIVE parameters used by current standalone logic.
# - Exposure/gain update flow follows AutoCameraSettings.

# Hard upper limit for exposure time (ms).
# 6000 us = 6 ms.
max_exposure_ms = 6.8

# Gain bounds and step (same meaning as AutoCameraSettings options).
# Repo-like defaults:
#   min_gain=1.0, max_gain=4.0, gain_change_step=0.5
min_gain_x = 1.0
# 4.0x maps to gain control value 400 in this standalone app.
max_gain_x = 4.0
gain_change_step_x = 0.5

# Mean method: cap per-frame change (fraction of current exposure).
mean_method_max_step_ratio = 0.12

# Percentile method: additional damping and step/deadband limits.
# Lower damping / smaller max step => more stable but slower adaptation.
percentile_damping = 0.29
percentile_max_step_ratio = 0.07
percentile_deadband_us = 75

# Hold gain for a few frames after each gain update to prevent ping-pong.
gain_cooldown_frames = 7
